/*
    DOM Exercise
    You have a list with elements. Some elements have the 'element' class. 
    - For the elements with 'element' class, add the element index after the text. Count just the elements with 'element' class when calculate the index.
    - Add a second button who changes the font-size of the list text, the font-family and the color.
*/
