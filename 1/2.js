/*
    DOM Exercise
    You have a list with elements. Some elements have the 'element' class. For the elements with 'element' class, add the element index after the text. Count just the elements 
    with 'element' class when calculate the index.
*/